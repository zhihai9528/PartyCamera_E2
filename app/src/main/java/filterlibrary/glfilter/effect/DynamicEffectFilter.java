package filterlibrary.glfilter.effect;

import android.content.Context;

import filterlibrary.glfilter.effect.bean.DynamicEffectData;

/**
 * 特效滤镜
 */
public class DynamicEffectFilter extends DynamicEffectBaseFilter {

    public DynamicEffectFilter(Context context, DynamicEffectData dynamicEffectData, String unzipPath) {
        super(context, dynamicEffectData, unzipPath);
    }
}
